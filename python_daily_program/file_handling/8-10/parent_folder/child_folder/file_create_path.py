import os 

os.chdir("parent_folder/child_folder") # Change the current working directory to the child folder inside the parent folder
file = open("demo.txt","x") # Create a new file named demo.txt in the child folder