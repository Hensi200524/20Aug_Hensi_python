import os

#os.mkdir("parent_folder") # Create the parent folder
#os.mkdir("new_folder") # Create the new folder

os.chdir("parent_folder") # Change the current working directory to the parent folder
os.mkdir("child_folder") # Create the child folder inside the parent folder